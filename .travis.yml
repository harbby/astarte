language: java
#jdk:
#- openjdk8

install:
  - wget https://apache.website-solution.net/maven/maven-3/3.6.3/binaries/apache-maven-3.6.3-bin.tar.gz
  - tar xzvf apache-maven-3.6.3-bin.tar.gz
  - export PATH=`pwd`/apache-maven-3.6.3/bin:$PATH
  - mvn -v

matrix:
  include:
    - jdk: openjdk8
      env: jdk=java8
    - jdk: openjdk11
      env: jdk=java11

sudo: required
dist: trusty
services:
  - docker

script:
  - mvn clean package test -Dlogback.configurationFile=`pwd`/build/ci-logback.xml --no-transfer-progress -Pjdk="$jdk"

before_deploy:
  - echo build astarte
before_cache:
  - rm -f  $HOME/.gradle/caches/modules-2/modules-2.lock
  - rm -fr $HOME/.gradle/caches/*/plugin-resolution/

after_success:
  - bash <(curl -s https://codecov.io/bash)

cache:
  directories:
    - "$HOME/.gradle/caches/"
    - "$HOME/.gradle/wrapper/"
